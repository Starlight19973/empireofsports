# Unity: Пошаговая установка на Windows

## Содержание
1. [Системные требования](#системные-требования)
2. [Шаг 1: Unity Hub](#шаг-1-unity-hub)
3. [Шаг 2: Unity Editor](#шаг-2-unity-editor)
4. [Шаг 3: IDE для кода](#шаг-3-ide-для-кода)
5. [JetBrains Rider: Полное руководство](#-jetbrains-rider-полное-руководство-для-unity)
6. [Claude Code в Rider](#-claude-code-в-jetbrains-rider)
7. [Шаг 4: Создание проекта](#шаг-4-создание-проекта)
8. [Шаг 5: Настройка Git](#шаг-5-настройка-git)
9. [Шаг 6: Дополнительные инструменты](#шаг-6-дополнительные-инструменты)
10. [Проверка установки](#проверка-установки)
11. [Первый скрипт](#первый-скрипт)

---

## Системные требования

### Минимальные
| Компонент | Требование |
|-----------|------------|
| ОС | Windows 10 (64-bit) |
| CPU | SSE2 поддержка |
| GPU | DirectX 10 совместимая |
| RAM | 8 GB |
| Диск | 20 GB свободно |

### Рекомендуемые (для комфортной работы)
| Компонент | Требование |
|-----------|------------|
| ОС | Windows 10/11 (64-bit) |
| CPU | Intel i5/AMD Ryzen 5 или лучше |
| GPU | NVIDIA GTX 1060 / AMD RX 580 или лучше |
| RAM | 16 GB+ |
| Диск | SSD, 50+ GB свободно |

---

## Шаг 1: Unity Hub

Unity Hub — менеджер для установки разных версий Unity и управления проектами.

### 1.1 Скачивание

1. Перейди на [unity.com/download](https://unity.com/download)
2. Нажми **"Download Unity Hub"**
3. Скачается файл `UnityHubSetup.exe` (~1.5 MB)

### 1.2 Установка

```
1. Запусти UnityHubSetup.exe
2. Прими лицензионное соглашение
3. Выбери папку установки (по умолчанию: C:\Program Files\Unity Hub)
4. Нажми "Install"
5. Дождись завершения
6. Запусти Unity Hub
```

### 1.3 Создание аккаунта Unity

```
1. В Unity Hub нажми "Sign in"
2. Выбери "Create account" (если нет аккаунта)
3. Заполни форму:
   - Email
   - Пароль
   - Имя пользователя
4. Подтверди email
5. Войди в аккаунт
```

### 1.4 Активация лицензии

```
1. Unity Hub → Preferences (шестерёнка)
2. Licenses → Add
3. Выбери "Get a free personal license"
4. Подтверди что доход < $200K/год
5. Лицензия активирована
```

---

## Шаг 2: Unity Editor

### 2.1 Выбор версии

**Рекомендую:** `Unity 6 (6000.0.x LTS)` или `Unity 2022.3.x LTS`

> LTS = Long Term Support (долгосрочная поддержка, стабильнее)

### 2.2 Установка

```
1. Unity Hub → Installs (левое меню)
2. Нажми "Install Editor"
3. Выбери версию (Unity 6 LTS рекомендуется)
4. Нажми "Install"
```

### 2.3 Выбор модулей

На экране выбора модулей отметь:

```
[x] Microsoft Visual Studio Community 2022  ← IDE для кода
[x] Windows Build Support (IL2CPP)          ← Для Windows-билдов
[x] WebGL Build Support                     ← Для браузерной версии
[ ] Android Build Support                   ← Позже, если нужно
[ ] iOS Build Support                       ← Не нужно на Windows
```

**Размер:** ~15-20 GB (с Visual Studio)

### 2.4 Дождись установки

Установка займёт 20-60 минут в зависимости от интернета.

---

## Шаг 3: IDE для кода

### Вариант A: Visual Studio 2022 (рекомендуется)

Если выбрал при установке Unity — уже установлен.

**Проверка:**
```
1. Открой Visual Studio Installer (поиск Windows)
2. Убедись что установлен "Game development with Unity" workload
3. Если нет — нажми Modify и добавь
```

### Вариант B: Visual Studio Code (легче)

```
1. Скачай: https://code.visualstudio.com/
2. Установи
3. Открой VS Code
4. Установи расширения (Ctrl+Shift+X):
   - C# (Microsoft)
   - Unity (Microsoft)
   - C# Dev Kit (Microsoft)
```

**Настройка в Unity:**
```
Unity → Edit → Preferences → External Tools
External Script Editor → выбери Visual Studio Code
```

### Вариант C: JetBrains Rider (лучший, но платный)

```
1. Скачай: https://www.jetbrains.com/rider/
2. 30 дней бесплатно
3. $149/год (бесплатно для студентов)
```

---

## 🚀 JetBrains Rider: Полное руководство для Unity

### Стоит ли качать Rider после установки Unity?

**Краткий ответ: ДА, если:**
- Ты серьёзно занимаешься разработкой (не хобби на пару часов в неделю)
- Готов платить $149/год или имеешь студенческую лицензию
- Ценишь скорость и продуктивность

**НЕТ, если:**
- Только начинаешь и хочешь бесплатный вариант → Visual Studio Community
- Простой проект → VS Code достаточно

### Rider vs Visual Studio: Сравнение для Unity

| Критерий | Rider | Visual Studio 2022 | VS Code |
|----------|-------|-------------------|---------|
| **Цена** | $149/год | Бесплатно (Community) | Бесплатно |
| **Анализ кода** | 1300+ правил | 600+ правил | Базовый |
| **Unity-плагин** | Встроенный, продвинутый | Требует установки | Расширения |
| **Производительность** | Быстрый | Тяжёлый | Очень лёгкий |
| **Рефакторинг** | Отличный | Хороший | Базовый |
| **Дебаггинг Unity** | Отличный | Хороший | Ограниченный |
| **Кросс-платформа** | Win/Mac/Linux | Win/Mac | Win/Mac/Linux |

### Почему Rider лучше для Unity

1. **Нативная интеграция с Unity**
   - Знает про `.meta` файлы
   - Подсветка MonoBehaviour методов (Update, Start, OnCollision...)
   - Автодополнение Unity API

2. **Умный анализ кода**
   - Находит NullReferenceException до запуска
   - Определяет неиспользуемый код
   - Подсказывает оптимизации

3. **Быстрая навигация**
   - Поиск по всему проекту (Shift+Shift)
   - Переход к определению (Ctrl+Click)
   - Поиск использований

4. **Отладка Unity**
   - Attach to Unity Player
   - Просмотр переменных в рантайме
   - Breakpoints в корутинах

### Установка Rider для Unity на Windows

#### Шаг 1: Скачивание
```
1. Перейди на https://www.jetbrains.com/rider/download/
2. Нажми "Download" (Windows)
3. Скачается JetBrains.Rider-20XX.X.X.exe (~700 MB)
```

#### Шаг 2: Установка
```
1. Запусти установщик
2. Выбери папку установки (по умолчанию: C:\Program Files\JetBrains\Rider)
3. Отметь опции:
   [x] Create Desktop shortcut (64-bit)
   [x] Add "Open Folder as Project" to context menu
   [x] .cs — Associate with Rider
4. Установи
5. Перезагрузи компьютер (рекомендуется)
```

#### Шаг 3: Активация
```
1. Запусти Rider
2. Войди в JetBrains Account (или создай новый)
3. Выбери лицензию:
   - 30-day trial — бесплатно
   - Student license — бесплатно (нужен .edu email)
   - Personal — $149/год
```

#### Шаг 4: Настройка для Unity
```
1. Rider → Settings (Ctrl+Alt+S)
2. Languages & Frameworks → Unity Engine
3. Убедись что:
   [x] Enable Unity plugin
   [x] Install Unity Editor plugin (for debugging)
```

#### Шаг 5: Подключение к Unity
```
1. Открой Unity Hub → твой проект
2. Unity → Edit → Preferences → External Tools
3. External Script Editor → выбери Rider
4. Теперь двойной клик на .cs файл откроет его в Rider
```

### Горячие клавиши Rider для Unity

| Клавиша | Действие |
|---------|----------|
| `Shift+Shift` | Поиск по всему проекту |
| `Ctrl+Shift+F` | Поиск текста в файлах |
| `Ctrl+Click` | Перейти к определению |
| `Alt+Enter` | Quick-fix / подсказки |
| `Ctrl+B` | Перейти к определению |
| `Ctrl+Alt+B` | Перейти к реализации |
| `F12` | Следующая ошибка |
| `Ctrl+D` | Дублировать строку |
| `Ctrl+/` | Закомментировать |
| `Ctrl+Shift+/` | Блочный комментарий |

---

## 🤖 Claude Code в JetBrains Rider

### Что такое Claude Code?

**Claude Code** — CLI-инструмент от Anthropic для AI-ассистированного программирования. Работает в терминале и может:
- Читать и понимать твой код
- Писать новый код
- Рефакторить существующий
- Отвечать на вопросы о проекте
- Запускать команды

### Можно ли запустить Claude Code в Rider?

**ДА!** Rider имеет встроенный терминал, в котором можно запустить Claude Code.

### Способ 1: Claude Code в терминале Rider

#### Установка Claude Code на Windows

```powershell
# 1. Установи Node.js (если ещё нет)
# Скачай с https://nodejs.org/ (LTS версию)

# 2. Установи Claude Code глобально
npm install -g @anthropic-ai/claude-code

# 3. Проверь установку
claude --version
```

#### Первый запуск
```powershell
# В терминале Rider (View → Tool Windows → Terminal)
claude

# Или в конкретной папке проекта
cd C:\Projects\Unity\EmpireOfSports
claude
```

#### Авторизация
```
1. При первом запуске откроется браузер
2. Войди в аккаунт Anthropic (claude.ai)
3. Разреши доступ
4. Вернись в терминал — готово!
```

### Способ 2: Claude Code Plugin для Rider

#### Установка плагина

```
1. Rider → Settings (Ctrl+Alt+S)
2. Plugins → Marketplace
3. Поиск: "Claude Code"
4. Установи "Claude Code [Beta]"
5. Перезапусти Rider
```

#### Использование плагина

| Горячая клавиша | Действие |
|-----------------|----------|
| `Ctrl+Esc` | Открыть Claude Code |
| | Diff-просмотр изменений |
| | Контекст текущего выделения |

#### Что умеет плагин:
- Запускает Claude Code в интегрированном терминале
- Показывает diff изменений прямо в IDE
- Передаёт контекст текущего файла/выделения
- Интеграция с редактором

### Известные проблемы на Windows

#### Проблема: Rider не определяется при `/ide`

```
Ошибка: "No IDE connection found"
```

**Решение:**
```powershell
# Запусти Rider, затем в терминале Rider:
claude /ide

# Если не работает, укажи путь вручную:
claude --ide-path "C:\Program Files\JetBrains\Rider 2025.1"
```

#### Проблема: Пути WSL vs Windows

```
Если используешь WSL, пути могут не совпадать:
Windows: C:\Projects\Unity
WSL: /mnt/c/Projects/Unity
```

**Решение:** Запускай Claude Code из нативного PowerShell в Rider, не из WSL-терминала.

### Workflow: Rider + Claude Code для Unity

#### Пример 1: Генерация скрипта
```
> claude

Claude: Чем могу помочь?

> Создай скрипт PlayerController.cs для Unity с управлением WASD,
  прыжком и спринтом. Используй новую Input System.

Claude: [генерирует код]
         [предлагает записать в файл]

> /accept
```

#### Пример 2: Рефакторинг
```
> Посмотри TennisBall.cs и оптимизируй физику мяча.
  Добавь эффект Магнуса для спина.

Claude: [читает файл]
         [предлагает изменения]
         [показывает diff]
```

#### Пример 3: Объяснение кода
```
> Объясни как работает AIOpponent.cs, особенно предсказание позиции мяча
```

### Настройка терминала в Rider для Claude Code

#### PowerShell (рекомендуется)
```
1. Rider → Settings → Tools → Terminal
2. Shell path: powershell.exe
   или: C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe
3. [x] Use ConPTY
```

#### Windows Terminal (альтернатива)
```
Shell path: wt.exe
```

### Tips & Tricks

#### 1. Быстрый доступ к терминалу
```
Alt+F12 — открыть/закрыть терминал в Rider
```

#### 2. Несколько терминалов
```
Можно открыть несколько вкладок терминала:
- Один для Claude Code
- Один для Git
- Один для Unity Build
```

#### 3. Контекст проекта
```
Claude Code автоматически видит структуру проекта.
Можно спросить:
> Какие скрипты отвечают за движение игрока?
> Покажи все классы, наследующие от MonoBehaviour
```

#### 4. Интеграция с MCP (новое в 2025)
```
Rider 2025.2+ имеет встроенный MCP Server.
Claude Code может подключаться к нему для глубокой интеграции.
```

---

## 📊 Сравнение: Rider + Claude Code vs Alternatives

| Вариант | Стоимость | AI-возможности | Unity-интеграция |
|---------|-----------|----------------|------------------|
| **Rider + Claude Code** | $149/год + Claude подписка | Отличные | Отличная |
| **VS Code + Copilot** | $10/мес | Хорошие | Средняя |
| **Visual Studio + Copilot** | $10/мес | Хорошие | Хорошая |
| **Cursor** | $20/мес | Отличные | Средняя |

### Рекомендация

**Для серьёзной Unity-разработки:**
1. Rider как основная IDE
2. Claude Code в терминале для сложных задач
3. GitHub Copilot как дополнение (опционально)

**Для начинающих/бюджетный вариант:**
1. VS Code (бесплатно)
2. Claude Code в терминале
3. C# Dev Kit расширение

---

## Шаг 4: Создание проекта

### 4.1 Новый проект

```
1. Unity Hub → Projects (левое меню)
2. Нажми "New project"
3. Выбери версию Unity (та что установил)
4. Выбери шаблон:

   Для спортивной игры:
   → "3D (Built-in Render Pipeline)" — проще, быстрее
   или
   → "3D (URP)" — красивее, чуть сложнее

5. Введи имя проекта: "EmpireOfSports"
6. Выбери папку (например: D:\Projects\Unity)
7. Нажми "Create project"
```

### 4.2 Первый запуск

Первый запуск проекта занимает 2-5 минут (импорт пакетов).

После загрузки увидишь:
- **Scene** — 3D вид сцены
- **Game** — как видит игрок
- **Hierarchy** — объекты на сцене
- **Inspector** — свойства объекта
- **Project** — файлы проекта
- **Console** — логи и ошибки

---

## Шаг 5: Настройка Git

### 5.1 Установка Git

```
1. Скачай: https://git-scm.com/download/win
2. Установи с настройками по умолчанию
3. Проверь в командной строке:
   git --version
```

### 5.2 Создание .gitignore

В корне проекта Unity создай файл `.gitignore`:

```gitignore
# Unity generated
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Uu]ser[Ss]ettings/

# MemoryCaptures
[Mm]emoryCaptures/

# Asset meta data
*.pidb
*.pdb
*.mdb

# Visual Studio
.vs/
*.csproj
*.sln
*.suo
*.tmp
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# Rider
.idea/

# OS generated
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Builds
*.apk
*.aab
*.exe
*.app

# Crashlytics
crashlytics-build.properties
```

### 5.3 Инициализация репозитория

```bash
cd D:\Projects\Unity\EmpireOfSports
git init
git add .
git commit -m "Initial commit: Unity project setup"
```

### 5.4 GitHub (опционально)

```bash
# Создай репозиторий на github.com, затем:
git remote add origin https://github.com/USERNAME/EmpireOfSports.git
git branch -M main
git push -u origin main
```

---

## Шаг 6: Дополнительные инструменты

### 6.1 GitHub Desktop (если не любишь консоль)

```
Скачай: https://desktop.github.com/
```

### 6.2 GitHub Copilot (автодополнение кода)

```
1. Подписка $10/мес: https://github.com/features/copilot
2. Установи расширение в VS Code / Visual Studio
3. Войди в GitHub аккаунт
```

### 6.3 Полезные Unity-пакеты

В Unity: **Window → Package Manager**

```
Рекомендую установить:
- Input System (новая система ввода)
- TextMeshPro (красивый текст)
- Cinemachine (камера)
- ProBuilder (быстрое прототипирование 3D)
```

---

## Проверка установки

### Чек-лист

```
[ ] Unity Hub установлен и запускается
[ ] Unity Editor установлен (видно в Installs)
[ ] Лицензия активирована (Personal)
[ ] Проект создаётся без ошибок
[ ] IDE открывает скрипты (двойной клик на .cs файл)
[ ] Git работает (git --version в консоли)
```

### Тест: создание первого объекта

```
1. В Unity: Hierarchy → + → 3D Object → Cube
2. Появился куб в сцене
3. Нажми Play (треугольник вверху)
4. Игра запустилась (куб виден в Game)
5. Нажми Play ещё раз чтобы остановить
```

---

## Первый скрипт

### Создание скрипта

```
1. В Project: правый клик → Create → C# Script
2. Назови "PlayerController"
3. Дождись компиляции (внизу справа)
4. Двойной клик — откроется в IDE
```

### Код для теста

```csharp
using UnityEngine;

public class PlayerController : MonoBehaviour
{
    public float speed = 5f;

    void Update()
    {
        // Получаем ввод
        float horizontal = Input.GetAxis("Horizontal"); // A/D или стрелки
        float vertical = Input.GetAxis("Vertical");     // W/S или стрелки

        // Двигаем объект
        Vector3 movement = new Vector3(horizontal, 0, vertical);
        transform.Translate(movement * speed * Time.deltaTime);
    }
}
```

### Применение скрипта

```
1. Сохрани скрипт (Ctrl+S в IDE)
2. В Unity: выбери Cube в Hierarchy
3. Перетащи скрипт PlayerController на Cube (или Add Component)
4. Нажми Play
5. Управляй кубом: W/A/S/D или стрелками
```

**Если работает — установка завершена!**

---

## Структура проекта

```
EmpireOfSports/
├── Assets/                 ← Все файлы игры
│   ├── Scenes/            ← Сцены (уровни)
│   ├── Scripts/           ← C# скрипты
│   ├── Prefabs/           ← Заготовки объектов
│   ├── Materials/         ← Материалы
│   ├── Textures/          ← Текстуры
│   ├── Models/            ← 3D модели
│   ├── Animations/        ← Анимации
│   └── Audio/             ← Звуки
├── Packages/              ← Unity пакеты
├── ProjectSettings/       ← Настройки проекта
└── .gitignore            ← Игнор для Git
```

---

## Горячие клавиши Unity

| Клавиша | Действие |
|---------|----------|
| `Ctrl+S` | Сохранить сцену |
| `Ctrl+P` | Play/Stop |
| `Ctrl+Shift+P` | Pause |
| `W` | Инструмент перемещения |
| `E` | Инструмент вращения |
| `R` | Инструмент масштабирования |
| `F` | Фокус на объекте |
| `Ctrl+D` | Дублировать объект |
| `Delete` | Удалить объект |
| `Ctrl+Z` | Отмена |

---

## Следующие шаги

После установки:

1. **Пройди туториал** — [learn.unity.com/pathway/unity-essentials](https://learn.unity.com/pathway/unity-essentials)
2. **Сделай Roll-a-Ball** — официальный туториал Unity (2 часа)
3. **Начни прототип тенниса** — спроси у Claude Code!

---

## Полезные ссылки

- [Unity Documentation](https://docs.unity3d.com/)
- [Unity Learn](https://learn.unity.com/)
- [Unity Asset Store](https://assetstore.unity.com/)
- [Unity Forum](https://forum.unity.com/)
- [r/Unity3D](https://reddit.com/r/Unity3D)

---

*Документ подготовлен: декабрь 2025*
